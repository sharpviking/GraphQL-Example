type Query {
  products: [Product]
  productByPrice(minPrice: Float!, maxPrice: Float!): [product]
  product(id: Id!): Product
}

type Product {
  id: ID!
  description: String!
  reviews: [Review]
  price: Float!
}

type Review {
  rating: Int
  comment: String
}
